{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "npx prisma db execute --file prisma/deploy.sql && node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
